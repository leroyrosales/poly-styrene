---
import Portfolio from "../components/Portfolio.astro";
import Skills from '../components/Skills.astro';
import People from '../components/People.astro';
import Outro from '../components/Outro.astro';
import Footer from "../components/Footer.astro";
import Navbar from "../components/Navbar.astro";

import site from "../data/site.json";
const { author, title, description, intro, likes } = site;

const randomLike = likes[Math.floor(Math.random()*likes.length)].toLowerCase();
const slogan = `I'm just a nerd that likes punk.`;

---

<html lang="en">
	<head>
		<meta charset="UTF-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<link rel="icon" type="image/x-icon" href="/favicon.ico" />
		<title>{ title } - { description }</title>
		<style is:global>
			@import "../styles/global.css";
		</style>
		<style>
			@import "../styles/devicon.min.css";
		</style>
	</head>

	<body>
	    <a href="#skills" class="sr-only focus:not-sr-only">
          Skip to skills
        </a>
		<Navbar/>
		<section class="py-24 px-12 border-2 border-black">
			<h1>👨🏾‍💻 {author}</h1>
			<p class="slogan anim-typewriter pb-8">{slogan}</p>
			<p class="text-3xl max-w-prose">{intro}</p>
		</section>
		<div class="grid grid-cols-1 lg:grid-cols-2 xl:grid-cols-3">
			<Portfolio/>
		</div>
		<Skills />
		<Outro />
		<section class="grid grid-cols-1 lg:grid-cols-2">
			<People />
			<Footer />
		</section>
		<!-- Latest version with linear easing only -->
		<!-- <script src="https://cdn.jsdelivr.net/npm/scrolltosmooth/dist/scrolltosmooth.pkgd.min.js"></script>
		<script>
			const anchorLinks = document.querySelectorAll("nav a");

			let smoothScroll = new scrollToSmooth(anchorLinks, {
				targetAttribute: 'href',
				duration: 2000,
				offset: 200,
				easing: easeInQuad,
			});

			smoothScroll.init();
		</script> -->
	</body>
</html>
